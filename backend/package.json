{
  "name": "backend",
  "version": "1.0.0",
  "main": "src/index.js",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsx watch src/index.ts",
    "build": "npx rimraf dist && tsc",
    "prod": "node dist/index.js",
    "db:push": "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/epbd1 drizzle-kit push",
    "db:generate": "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/epbd1 drizzle-kit generate",
    "db:migrate": "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/epbd1 drizzle-kit migrate",
    "db:studio": "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/epbd1 drizzle-kit studio"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "@types/node": "^24.10.1",
    "drizzle-kit": "^0.31.7",
    "prisma": "^6.17.1",
    "rimraf": "^6.1.0",
    "ts-node-dev": "^2.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.44.7",
    "express": "^5.1.0",
    "postgres": "^3.4.7",
    "zod": "^4.1.12"
  }
}
