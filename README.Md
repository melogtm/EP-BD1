## 🚀 Como Rodar o Projeto

### Backend

Para instruções completas sobre como rodar o backend, acesse o **README.md** na pasta `backend/`:

```bash
cd backend
cat README.md
```

O backend roda por padrão em `http://localhost:3000`

---

### Frontend

#### 1. Instalar dependências

```bash
npm i
```

#### 2. Rodar em desenvolvimento

```bash
npm run dev
```

O frontend abrirá em `http://localhost:8080`
